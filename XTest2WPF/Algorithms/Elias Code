Кодирование:
	input -> p
 1)Задается двумерный массив размером 6х6:
 	p = array[i][j] 
 2)с - это часть массива р (двумерный массив, который заполняется рандомно 0 и 1):
 	c = array[5][5] 
 //6-й столбец и 6-я строка двумерного массива р пока пусты
		(пользователь должен заполнить)
 	p[i][6] == NULL 
 	p[6][j] == NULL
  
	if(p[i] -> sum(numbers)%2==0) 
		-> p[i][6] == 0
	else 
 		-> p[i][6] == 1
 	if(p[j] -> sum(numbers)%2==0)
		-> p[6][j] == 0
	else
		-> p[6][j] == 1
 Декодирование:
 1)В двумерном массиве с[5][5] допущена ошибка на позиции [i][j].
 2)Чтобы найти ошибку нужно проверить каждый столбец и строку на четность, как это делали в кодировании.
 	if((p[i] -> sum(numbers)%2 == 0) &(p[i][6] ==! 0)) --> значит ошибка в этой строке
  else
      if((p[i] -> sum(numbers)%2 == 1) &(p[i][6] ==! 1)) --> ошибка в этой строке
      
3) Теперь находим ошибку в столбце.
 
  if((p[j] -> sum(numbers)%2==0) &(p[6][j] ==!0 )) --> ошибка в этом столбце
  else
       if((p[j] -> sum(numbers)%2==1) &(p[6][j] ==! 1)) --> ошибка в этом столбце
       
4) Итак, мы имеем строку и столбец, в которых допущена ошибка. Находим клеточку с позицией [i][j], где значение i соответствует строке, а значение j -- столбцу. Мы нашли ошибку, которую пользователь должен исправить.
  if(p[i][j] == 0)
      --> 1
   else
      --> 0
